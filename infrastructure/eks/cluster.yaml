apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: container-orch-cluster
  region: us-east-1

managedNodeGroups:
  - name: container-orch-nodes
    instanceType: t3.small

    desiredCapacity: 2
    minSize: 2
    maxSize: 3

    volumeSize: 20
    volumeType: gp3